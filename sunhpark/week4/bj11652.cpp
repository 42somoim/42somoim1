#include <iostream>
#include <algorithm>
using		namespace std;

int			N;

int			main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	long	long	num[100001];

	cin >> N;
	for (int i = 0; i < N; i++)
	{
		cin >> num[i];

	return (0);
}
